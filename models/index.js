const {listContacts, getById, addContact, renewContact, deleteContact} = require("./contacts");

module.exports = {
    listContacts,
    getById,
    addContact,
    renewContact,
    deleteContact
}